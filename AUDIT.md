# Repository Audit — personal-website-2026

Generated by static analysis of the repository at commit `2015224` (branch `main`). Scope excludes `node_modules`, `dist`, `.git`, and lockfiles per instructions; `package-lock.json` was read only to confirm the package manager and pinned versions, not enumerated as content.

**Preliminary note on scope mismatch:** the audit brief assumes a Vite-based React app (`vite.config.*`, `index.html`, `src/assets/`, MDX content, a "gift-registry mockup" and "CompuFaster mockup" gallery item, a Spanish caption "Lo mesa de regalos perfecto"). None of these exist in this repository. This is a **Next.js App Router** project. Every section below states "not present" explicitly where the requested artifact does not exist, per the brief's own instructions, rather than silently substituting the closest analog.

---

## 1. Stack and build

- **Framework:** Next.js (App Router), not Vite. No `vite.config.*` file exists anywhere in the repo.
- **Next.js version:** `16.1.6` (`package.json:13`, confirmed installed at `node_modules/next/package.json`)
- **UI library:** React `19.2.3` / React DOM `19.2.3` (`package.json:14-15`)
- **Language:** TypeScript (`.ts`/`.tsx` throughout `app/`, `components/`; `tsconfig.json` present). One plain-JS-adjacent file: `data/portfolioData.json` (JSON, not JS/TS).
- **Package manager:** npm — `package-lock.json` is present at repo root; no `yarn.lock`, `pnpm-lock.yaml`, or `bun.lockb` exist.

### Dependencies, grouped

**Framework**
| Package | Version | File |
|---|---|---|
| `next` | `16.1.6` | `package.json:13` |
| `react` | `19.2.3` | `package.json:14` |
| `react-dom` | `19.2.3` | `package.json:15` |

**Styling**
| Package | Version | File |
|---|---|---|
| `tailwindcss` | `^4` (devDependency) | `package.json:24` |
| `@tailwindcss/postcss` | `^4` (devDependency) | `package.json:18` |

Note: see [Section 6](#6-styling-system) — Tailwind is installed and wired into PostCSS but is not actually used by any file in the codebase.

**Animation**
- Not present. No animation library (`framer-motion`, `gsap`, `motion`, etc.) appears in `package.json` dependencies or devDependencies.

**Content**
- Not present as a CMS/MDX package. Content is a static local JSON file (`data/portfolioData.json`) imported directly — no `@next/mdx`, `contentlayer`, `next-mdx-remote`, or headless-CMS SDK is installed.
- `lucide-react` `^0.577.0` (`package.json:12`) is used for inline iconography, not content.

**Tooling**
| Package | Version | File |
|---|---|---|
| `typescript` | `^5` (devDependency) | `package.json:25` |
| `eslint` | `^9` (devDependency) | `package.json:22` |
| `eslint-config-next` | `16.1.6` (devDependency) | `package.json:23` |
| `@types/node` | `^20` (devDependency) | `package.json:19` |
| `@types/react` | `^19` (devDependency) | `package.json:20` |
| `@types/react-dom` | `^19` (devDependency) | `package.json:21` |

### Config file contents

`vite.config.*` — **not present**.

`next.config.ts` (verbatim, `next.config.ts:1-9`):
```ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  turbopack: {
    root: process.cwd(),
  },
};

export default nextConfig;
```

`tsconfig.json` (verbatim):
```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "exclude": ["node_modules"]
}
```

`postcss.config.mjs` (verbatim):
```js
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
```

`tailwind.config.*` — **not present** (Tailwind v4 does not require a JS config file; no `@theme` CSS-based config exists either — see Section 6).

`eslint.config.mjs` (verbatim):
```js
import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
```

### Deployment config

- `vercel.json` — **not present**
- `netlify.toml` — **not present**
- `.github/workflows/` — **not present** (no `.github` directory exists at all)
- `README.md` references deploying "on Vercel" (`README.md:32-36`) but this is boilerplate text from `create-next-app`, not an actual deployment config file.

### Router / routes

- No routing library is installed (`react-router`, `react-router-dom`, `@tanstack/router`, etc. are absent from `package.json`).
- Next.js App Router is present by virtue of the framework itself, but only **one route exists**: `app/page.tsx` (mapped to `/`). Confirmed via full listing of `app/`: `app/globals.css`, `app/icon.svg`, `app/layout.tsx`, `app/page.tsx` — no nested route segment folders (no other `page.tsx` anywhere in `app/`).

---

## 2. File tree

Full tree to depth 4 (excluding `node_modules`, `dist`, `.git`, lockfiles):

```
.
├── .gitignore
├── README.md                              — boilerplate create-next-app readme
├── app/                                   — Next.js App Router entry point (single route)
│   ├── globals.css                        — global reset + body background gradient, CSS custom properties
│   ├── icon.svg                           — App Router favicon convention (auto-served as /icon.svg)
│   ├── layout.tsx                         — root layout: loads Geist fonts, sets <html lang>, page <title>/description metadata
│   └── page.tsx                           — the only route ("/"); renders <PortfolioSection>
├── components/
│   └── portfolio/                         — all UI code for the single-page portfolio
│       ├── PortfolioSection.module.css    — the entire site's styling (CSS Module, ~1017 lines)
│       ├── PortfolioSection.tsx           — page-level layout composing all cards + gallery
│       ├── components/                    — presentational subcomponents
│       │   ├── DesignGallery.tsx          — gallery grid + lightbox state owner
│       │   ├── ExperienceCard.tsx         — renders the experience timeline
│       │   ├── GalleryCard.tsx            — single gallery grid tile
│       │   ├── GalleryLightbox.tsx        — modal image viewer (portal, keyboard nav)
│       │   ├── LocationCard.tsx           — "Based In" card wrapper
│       │   ├── LocationMap.tsx            — OpenStreetMap iframe embed
│       │   ├── ProjectCard.tsx            — featured/secondary project link tile
│       │   ├── SocialLinks.tsx            — header social icon row
│       │   ├── StackToolsCard.tsx         — "Stack & Tools" card wrapper
│       │   ├── StudyCaseCard.tsx          — featured study case (Alexandria) hero card
│       │   └── ToolsIconList.tsx          — icon chip list used inside StackToolsCard
│       ├── iconMap.ts                     — maps string keys to lucide-react icon components + icon-source-type helpers
│       ├── socialIconMap.tsx              — maps social platform keys to icon components (incl. inline Behance SVG)
│       └── types.ts                       — all TypeScript interfaces for the portfolio data shape
├── data/
│   └── portfolioData.json                 — single source of truth for all page content
├── eslint.config.mjs                      — ESLint flat config (Next core-web-vitals + TS rules)
├── next.config.ts                         — Next.js config (Turbopack root only)
├── package-lock.json                      — npm lockfile (excluded from content review per scope)
├── package.json                           — dependency manifest, npm scripts
├── postcss.config.mjs                     — PostCSS config wiring `@tailwindcss/postcss`
├── public/                                — static assets served at site root
│   ├── file.svg, globe.svg, next.svg, vercel.svg, window.svg  — unused create-next-app starter icons
│   ├── images/
│   │   ├── gallery/                       — 47 Design Gallery source images (PNG/JPG/SVG)
│   │   ├── icons/
│   │   │   ├── backend/                   — 6 SVG tech-stack icons (all referenced)
│   │   │   ├── design/                    — 6 SVG tool icons (1 unreferenced — see Section 5)
│   │   │   ├── frontend/                  — 7 SVG tech-stack icons (1 unreferenced — see Section 5)
│   │   │   └── social/                    — 4 SVG social-platform icons (all referenced)
│   │   └── portfolio/                     — 10 files: project icons, a duplicate "featured project" image pair, and 2 unreferenced SVGs
│   ├── next.svg, vercel.svg, window.svg   — (see above, create-next-app defaults)
│   └── window.svg
└── tsconfig.json
```

---

## 3. Content data model — highest priority section

All portfolio content lives in **one JSON file**: `data/portfolioData.json`, typed by `components/portfolio/types.ts` and imported directly (no fetch, no CMS, no MDX) in both `app/layout.tsx:3` and `app/page.tsx:2`:

```ts
import portfolioData from "@/data/portfolioData.json";
```

### Experience timeline entries

- **File:** `data/portfolioData.json` (array at `data/portfolioData.json:147-183`)
- **Type:** TS interface `ExperienceEntry`, `components/portfolio/types.ts:59-65`:
```ts
export interface ExperienceEntry {
  startDate: string;
  endDate: string;
  role: string;
  company: string;
  description?: string;
}
```
- **Example entry** (`data/portfolioData.json:148-154`, AVIANCA LifeMiles):
```json
{
  "startDate": "Jul 2025",
  "endDate": "Present",
  "role": "Frontend Developer (React)",
  "company": "AVIANCA LifeMiles",
  "description": "Building flows, reusable UI patterns, and scalable architecture across loyalty product experiences."
}
```
- All five roles are present as plain object literals in this array: AVIANCA LifeMiles (`data/portfolioData.json:148-154`), Conexion El Salvador (`data/portfolioData.json:155-161`, note: no accent on "Conexion" in the data, differing from "Conexión" in the brief), Iluminodes (`data/portfolioData.json:162-168`, note: spelled "Iluminodes" not "Illuminodes"), Colegio Salarrue (`data/portfolioData.json:169-175`, no accent on "Salarrué"), and Cellworld (`data/portfolioData.json:176-182`, one word, not "Cell World").
- Rendered by `components/portfolio/components/ExperienceCard.tsx:17-37` (`.map` over `entries`).

### Project cards

- **File:** `data/portfolioData.json`, split across three separate arrays/objects — not one uniform list:
  - `featuredProjects` (Typo, Silver Voluntarios, Cabuda): `data/portfolioData.json:194-219`
  - `secondaryProjects` (Auralis, Commonplace): `data/portfolioData.json:220-237`
  - `featuredStudyCase` (Alexandria only): `data/portfolioData.json:184-193`
- **Type:** `ProjectItem`, `components/portfolio/types.ts:67-74`:
```ts
export interface ProjectItem {
  title: string;
  year: string;
  category: string;
  icon: string;
  href: string;
  imageAlt: string;
}
```
  Alexandria uses the extended interface `FeaturedStudyCase`, `components/portfolio/types.ts:76-79`:
```ts
export interface FeaturedStudyCase extends ProjectItem {
  label: string;
  description?: string;
}
```
- **Example entry — Typo** (`data/portfolioData.json:195-202`):
```json
{
  "title": "Typo",
  "year": "2025",
  "category": "Frontend",
  "icon": "/images/portfolio/typo.svg",
  "href": "https://typo-nu.vercel.app/",
  "imageAlt": "Typo project thumbnail."
}
```
- **Example entry — Alexandria (featuredStudyCase)** (`data/portfolioData.json:184-193`):
```json
{
  "label": "",
  "title": "Alexandria",
  "year": "2026",
  "category": "UI/UX, Frontend Development",
  "icon": "",
  "href": "https://alexandria-landing-page.vercel.app/",
  "imageAlt": "Alexandria project thumbnail.",
  "description": "Minimalist note-taking tool."
}
```
  Note: `label` and `icon` are both empty strings here — see Section 8 and Section 10 for the consequences.
- Rendered by `components/portfolio/components/ProjectCard.tsx` (featured/secondary) and `components/portfolio/components/StudyCaseCard.tsx` (Alexandria).

### Design Gallery items

- **File:** `data/portfolioData.json`, object `designGallery` at `data/portfolioData.json:238-714`, items array at `data/portfolioData.json:241-712`.
- **Type:** `DesignGalleryItem`, `components/portfolio/types.ts:41-51`:
```ts
export interface DesignGalleryItem {
  id: string;
  title: string;
  category: string;
  image: string;
  alt: string;
  href?: string;
  featured: boolean;
  width: number;
  height: number;
}
```
- **Example entry** (`data/portfolioData.json:243-251`):
```json
{
  "id": "a4-page-design",
  "title": "A4 Page Design",
  "category": "Editorial Design",
  "image": "/images/gallery/a4-page-design.png",
  "alt": "Editorial A4 page design with a structured print-inspired composition.",
  "featured": false,
  "width": 1200,
  "height": 1600
}
```
- Rendered by `components/portfolio/components/DesignGallery.tsx:28-36` → `GalleryCard.tsx` (grid) and `components/portfolio/components/GalleryLightbox.tsx` (modal).

### Stack & Tools icon list

- **File:** `data/portfolioData.json`, object `stackTools` at `data/portfolioData.json:48-146`.
- **Type:** `StackToolsSection` / `StackToolItem`, `components/portfolio/types.ts:26-39`:
```ts
export interface StackToolItem {
  name: string;
  icon: string;
  href?: string;
  ariaLabel: string;
}

export interface StackToolsSection {
  title: string;
  frontend?: StackToolItem[];
  developer?: StackToolItem[];
  backend?: StackToolItem[];
  designer?: StackToolItem[];
}
```
- **Example entry** (`data/portfolioData.json:51-55`):
```json
{
  "name": "React Vite",
  "icon": "/images/icons/frontend/akar-icons_react-fill.svg",
  "ariaLabel": "React Vite"
}
```
- Grouped into `frontend`/`backend`/`designer` sub-arrays (6 items each) and rendered by `components/portfolio/components/StackToolsCard.tsx:10-26` → `ToolsIconList.tsx`.

### Header bio paragraph

- **File:** `data/portfolioData.json:4`, field `profile.description`.
- **Type:** part of `PortfolioProfile`, `components/portfolio/types.ts:1-4`:
```ts
export interface PortfolioProfile {
  name: string;
  description: string;
}
```
- **Verbatim value** (`data/portfolioData.json:4`):
```
"Fullstack developer focused on building clear, functional, and scalable digital products. Turning complex ideas into thoughtful interfaces with strong visual systems and solid frontend architecture."
```
- Rendered at `components/portfolio/PortfolioSection.tsx:40`: `<p className={styles.heroDescription}>{profile.description}</p>`. Also reused as the page `<meta name="description">` via `app/layout.tsx:21` (`description: portfolioContent.profile.description`).

---

## 4. Gallery inventory

**Total item count: 47** (verified: `data.designGallery.items.length === 47`, and every file in `public/images/gallery/` has exactly one corresponding JSON entry — no orphaned files, no dangling references).

No item has a `caption` field or a `tag` field in the schema — only `title`, `category`, and `alt` exist (see `DesignGalleryItem` in Section 3). `category` doubles as the closest thing to a tag.

| Index | Image filename | Title | Category |
|---|---|---|---|
| 0 | `a4-page-design.png` | A4 Page Design | Editorial Design |
| 1 | `admin-dashboard-day.png` | Admin Dashboard Day | Dashboard UI |
| 2 | `admin-dashboard-night.png` | Admin Dashboard Night | Dashboard UI |
| 3 | `alexandria-bento-01.png` | Alexandria Bento | UI/UX Design |
| 4 | `alexandria-beta-landing.png` | Alexandria Beta Landing | Landing Page Design |
| 5 | `alexandria-import-file-modal.png` | Alexandria Import File Modal | UI Component |
| 6 | `alexandria-settings-modal.png` | Alexandria Settings Modal | UI Component |
| 7 | `auralis-social-media-summer-offer.png` | Auralis Summer Offer | Social Media Design |
| 8 | `banner-design.png` | Banner Design | Graphic Design |
| 9 | `biterbit-iso.png` | Biterbit Iso | Brand Identity |
| 10 | `biterbit-isologo.png` | Biterbit Isologo | Brand Identity |
| 11 | `cabuda-mock.png` | Cabuda Mock | Product Mockup |
| 12 | `cabuda-mocks.png` | Cabuda Mocks | Product Mockup |
| 13 | `commonplace-landing.png` | Commonplace Landing | Landing Page Design |
| 14 | `commonplace-mocks.png` | Commonplace Mocks | Product Mockup |
| 15 | `daedalus-branding.png` | Daedalus Branding | Brand Identity |
| 16 | `data-graph.svg` | Data Graph | Data Visualization |
| 17 | `deepcloud-isologo-design.png` | Deepcloud Isologo Design | Brand Identity |
| 18 | `ecommerce-grid-layout.png` | Ecommerce Grid Layout | E-commerce UI |
| 19 | `ecommerce-landing.png` | Ecommerce Landing | Landing Page Design |
| 20 | `event-landing.png` | Event Landing | Landing Page Design |
| 21 | `financial-markets-landing.png` | Financial Markets Landing | Landing Page Design |
| 22 | `flowers.png` | Flowers | Visual Exploration |
| 23 | `generic-landing-02.png` | Generic Landing 02 | Landing Page Design |
| 24 | `generic-landing.png` | Generic Landing | Landing Page Design |
| 25 | `internet-day-beta-landing.png` | Internet Day Beta Landing | Landing Page Design |
| 26 | `james-webb-landing.png` | James Web Landing | Landing Page Design |
| 27 | `landingPage.png` | Landing Page | Landing Page Design |
| 28 | `magazine-cover.jpg` | Magazine Cover | Editorial Design |
| 29 | `magazine-mock-02.png` | Magazine Mock 02 | Editorial Mockup |
| 30 | `magazine-mock.png` | Magazine Mock | Editorial Mockup |
| 31 | `math-dashboard.png` | Math Dashboard | Dashboard UI |
| 32 | `mobile-app-mock.png` | Mobile App Mock | Mobile UI |
| 33 | `nasa-dashboard.png` | Nasa Dashboard | Dashboard UI |
| 34 | `nothing-phone-2a-02.png` | Nothing Phone 2A 02 | Product Mockup |
| 35 | `nothing-phone-2a.png` | Nothing Phone 2A | Product Mockup |
| 36 | `organization-dashboard.png` | Organization Dashboard | Dashboard UI |
| 37 | `paper-mocks.png` | Paper Mocks | Editorial Mockup |
| 38 | `scrapped-a4-design.png` | Scrapped A4 Design | Editorial Design |
| 39 | `scrapped-glow-landing.png` | Scrapped Glow Landing | Landing Page Design |
| 40 | `scraps-02.png` | Scraps 02 | Visual Exploration |
| 41 | `silver-voluntarios-mock.png` | Silver Voluntarios Mock | Product Mockup |
| 42 | `social-media-banner.png` | Social Media Banner | Social Media Design |
| 43 | `social-media-dark-mode.png` | Social Media Dark Mode | Social Media Design |
| 44 | `social-media-design.png` | Social Media Design | Social Media Design |
| 45 | `sputnik-poster.png` | Sputnik Poster | Poster Design |
| 46 | `student-management-dashboard.png` | Student Management Dashboard | Dashboard UI |

### Duplicate / near-duplicate flags

Checked by MD5 content hash across `public/images/gallery/` — **no two gallery files are byte-identical**. (One exact byte-identical duplicate does exist in the repo, but outside the gallery folder — see Section 5: `public/images/portfolio/featured-project.png` and `public/images/portfolio/freatured-project.png`.)

Near-duplicates by subject (same product/theme, different file), all confirmed present:
- `admin-dashboard-day.png` (index 1) / `admin-dashboard-night.png` (index 2) — day/night variants of the same dashboard.
- `cabuda-mock.png` (index 11) / `cabuda-mocks.png` (index 12).
- `commonplace-landing.png` (index 13) / `commonplace-mocks.png` (index 14).
- `generic-landing.png` (index 24) / `generic-landing-02.png` (index 23).
- `magazine-mock.png` (index 30) / `magazine-mock-02.png` (index 29).
- `nothing-phone-2a.png` (index 35) / `nothing-phone-2a-02.png` (index 34).
- `biterbit-iso.png` (index 9) / `biterbit-isologo.png` (index 10).

Regarding the specific expected duplicates named in the brief:
- **Daedalus striped graphic:** only one Daedalus item exists — `daedalus-branding.png` (index 15). No duplicate of it is present.
- **Teal analytics dashboard:** no gallery item title, category, or filename references "teal" or a matching analytics dashboard beyond the generic `math-dashboard.png` (31), `nasa-dashboard.png` (33), `admin-dashboard-day/night.png` (1, 2), `organization-dashboard.png` (36), and `student-management-dashboard.png` (46) — **not present** as a distinct duplicated item.
- **CompuFaster mockup:** **not present**. No file or JSON entry anywhere in the repository references "CompuFaster" (grep across the full repo returned zero matches outside this audit file).
- **Dark 3D-lettering piece:** **not present** by that description; no item title or category references 3D lettering.
- **Gift-registry mockup:** **not present**. No file or JSON entry anywhere references "gift" or "registry".

---

## 5. Image assets

There is no `src/assets/` directory in this repository — all images live under `public/images/`. Dimensions below were read from each file's own header (PNG IHDR / JPEG SOF marker), not from the JSON's declared `width`/`height` fields, so declared-vs-actual can be compared.

### `public/images/gallery/` (47 files)

| Filename | Format | Actual dimensions | Size (KB) | Over 300 KB? |
|---|---|---|---|---|
| a4-page-design.png | PNG | 2418×3422 | 103 | |
| admin-dashboard-day.png | PNG | 4638×2796 | 2935 | ✓ |
| admin-dashboard-night.png | PNG | 4638×2796 | 3088 | ✓ |
| alexandria-bento-01.png | PNG | 1024×768 | 227 | |
| alexandria-beta-landing.png | PNG | 3840×2368 | 5843 | ✓ |
| alexandria-import-file-modal.png | PNG | 924×846 | 87 | |
| alexandria-settings-modal.png | PNG | 942×932 | 60 | |
| auralis-social-media-summer-offer.png | PNG | 1024×768 | 1153 | ✓ |
| banner-design.png | PNG | 1920×1080 | 2502 | ✓ |
| biterbit-iso.png | PNG | 2048×1536 | 969 | ✓ |
| biterbit-isologo.png | PNG | 2048×1536 | 59 | |
| cabuda-mock.png | PNG | 1920×1254 | 1408 | ✓ |
| cabuda-mocks.png | PNG | 3398×2548 | 1381 | ✓ |
| commonplace-landing.png | PNG | 1240×834 | 187 | |
| commonplace-mocks.png | PNG | 1024×656 | 408 | ✓ |
| daedalus-branding.png | PNG | 1024×768 | 945 | ✓ |
| data-graph.svg | SVG | n/a (vector) | 1028 | ✓ |
| deepcloud-isologo-design.png | PNG | 2048×1536 | 490 | ✓ |
| ecommerce-grid-layout.png | PNG | 1440×2569 | 1683 | ✓ |
| ecommerce-landing.png | PNG | 3840×10146 | 8513 | ✓ |
| event-landing.png | PNG | 3840×6827 | 15244 | ✓ |
| financial-markets-landing.png | PNG | 1920×1694 | 424 | ✓ |
| flowers.png | PNG | 1440×3046 | 3018 | ✓ |
| generic-landing-02.png | PNG | 1920×1365 | 2338 | ✓ |
| generic-landing.png | PNG | 2880×2048 | 2418 | ✓ |
| internet-day-beta-landing.png | PNG | 3840×11034 | 30515 | ✓ |
| james-webb-landing.png | PNG | 1440×5023 | 5102 | ✓ |
| landingPage.png | PNG | 1920×1317 | 394 | ✓ |
| magazine-cover.jpg | JPEG | 2035×2880 | 1350 | ✓ |
| magazine-mock-02.png | PNG | 4500×3000 | 13272 | ✓ |
| magazine-mock.png | PNG | 4500×3000 | 9934 | ✓ |
| math-dashboard.png | PNG | 1440×940 | 156 | |
| mobile-app-mock.png | PNG | 4500×3000 | 2610 | ✓ |
| nasa-dashboard.png | PNG | 2880×2500 | 6001 | ✓ |
| nothing-phone-2a-02.png | PNG | 1500×1324 | 461 | ✓ |
| nothing-phone-2a.png | PNG | 1497×1498 | 2003 | ✓ |
| organization-dashboard.png | PNG | 1920×1139 | 181 | |
| paper-mocks.png | PNG | 5000×3750 | 17755 | ✓ |
| scrapped-a4-design.png | PNG | 2418×3422 | 142 | |
| scrapped-glow-landing.png | PNG | 2880×2048 | 5367 | ✓ |
| scraps-02.png | PNG | 2990×1920 | 2025 | ✓ |
| silver-voluntarios-mock.png | PNG | 1883×1034 | 188 | |
| social-media-banner.png | PNG | 3204×1219 | 1191 | ✓ |
| social-media-dark-mode.png | PNG | 3840×2160 | 2747 | ✓ |
| social-media-design.png | PNG | 1920×1080 | 3150 | ✓ |
| sputnik-poster.png | PNG | 1920×1354 | 3956 | ✓ |
| student-management-dashboard.png | PNG | 1920×1080 | 119 | |

**Total uncompressed weight of all 47 gallery images combined: 169,091,117 bytes ≈ 165,128 KB ≈ 161.3 MB.**

**36 of 47 files exceed 300 KB** (see "Over 300 KB?" column above). The heaviest single file is `internet-day-beta-landing.png` at ~30.5 MB, followed by `paper-mocks.png` (~17.8 MB) and `event-landing.png` (~14.9 MB).

Note the JSON's declared `width`/`height` (Section 3 schema) do not match actual pixel dimensions in most cases — e.g. `a4-page-design` is declared `1200×1600` (`data/portfolioData.json:249-250`) but the file is actually `2418×3422`; `admin-dashboard-day` is declared `1440×1024` (`data/portfolioData.json:259-260`) but the file is actually `4638×2796`. Since `GalleryCard.tsx:29-30` and `GalleryLightbox.tsx:168-169` pass these declared values straight to Next.js `<Image width={} height={}>`, the aspect ratio used for layout does not match the source file's real aspect ratio in most entries.

### `public/images/icons/` (23 files, all SVG, sizes 233 B–9.4 KB — none exceed 300 KB)

- `backend/` — 6 files, all referenced in `data/portfolioData.json` `stackTools.backend`.
- `frontend/` — 7 files; 6 referenced in `stackTools.frontend`. `la_sass.svg` is **not referenced anywhere** in the codebase (confirmed via full-repo grep) — an orphaned asset.
- `design/` — 6 files; 5 referenced in `stackTools.designer` (Illustrator uses the string `"illustrator"` mapped via `iconMap.ts`, not an SVG file). `vscode-icons_file-type-affinity.svg` is **not referenced anywhere** — an orphaned asset.
- `social/` — 4 files, all referenced in `data/portfolioData.json` `socialLinks`.

### `public/images/portfolio/` (10 files)

| Filename | Size (KB) | Referenced in code? |
|---|---|---|
| alexandria.svg | 1.0 | **No** — `featuredStudyCase.icon` is `""` (`data/portfolioData.json:189`), and the Alexandria card's `<Image>` render is commented out (`StudyCaseCard.tsx:33-41`) |
| auralis.svg | 0.6 | Yes — `secondaryProjects[0].icon` |
| cabuda.svg | 0.7 | Yes — `featuredProjects[2].icon` |
| commonplace.svg | 0.3 | Yes — `secondaryProjects[1].icon` |
| featured-project.png | 109.7 | Yes — CSS background in `PortfolioSection.module.css:558` |
| freatured-project.png | 109.7 | **No** — byte-identical duplicate of `featured-project.png` (confirmed via MD5 hash), unreferenced, appears to be a misspelled leftover copy |
| map-santa-ana.svg | 395.6 | **No** — the live "Based In" map uses an OpenStreetMap iframe (`LocationMap.tsx`), not this static SVG |
| northline.svg | 0.4 | **No** — no "northline" entry exists anywhere in `data/portfolioData.json` |
| silver-voluntarios.svg | 12.7 | Yes — `featuredProjects[1].icon` |
| typo.svg | 19.0 | Yes — `featuredProjects[0].icon` |

`map-santa-ana.svg` (395.6 KB) exceeds 300 KB despite being unused.

### Format / responsive-image techniques

- **No AVIF or WebP files exist** anywhere in the repository (confirmed by extension scan of every image directory).
- **No `srcset` and no `<picture>` element appear anywhere in the codebase** (confirmed via full-repo grep for `srcSet`, `<picture`, `.webp`, `.avif` — zero matches). All images render through Next.js's built-in `<Image>` component (`next/image`), which generates responsive `srcset`/format negotiation automatically at runtime via Next's image optimizer — but this is framework-level behavior, not something authored in source, and no explicit `images` config exists in `next.config.ts` to customize it.
- `loading="lazy"` appears **2 times** in the codebase: `components/portfolio/components/GalleryCard.tsx:31` (on the `<Image>`) and `components/portfolio/components/LocationMap.tsx:41` (on the map `<iframe>`, not an image element).
- `decoding="async"` appears **0 times** anywhere in the codebase.
- `width`/`height` attributes: every `<Image>` element in the codebase supplies both (Next.js requires this unless `fill` is used) — **5 rendered `<Image>` elements total**, all with explicit `width`/`height`: `ToolsIconList.tsx:29-36`, `GalleryLightbox.tsx:165-173`, `ProjectCard.tsx:32-38`, `GalleryCard.tsx:26-34`, `SocialLinks.tsx:32-39`. A sixth `<Image>` in `StudyCaseCard.tsx:34-40` exists in source but is wrapped in a JSX comment (`{/* ... */}`) and never renders.
- `alt` attributes: all 5 rendered `<Image>` elements have an `alt` prop. 2 are empty strings (`alt=""` at `ToolsIconList.tsx:31` and `SocialLinks.tsx:34`, both paired with `aria-hidden="true"` — correct usage for decorative icons where the parent link already carries an `aria-label`). The other 3 use content-derived alt text (`item.alt`, `selectedItem.alt`, `project.imageAlt`).

---

## 6. Styling system

### Approach in use

**Plain CSS via a single CSS Module** (`components/portfolio/PortfolioSection.module.css`, 1017 lines) plus one small global stylesheet (`app/globals.css`, 36 lines) for resets and body background. No `styled-components`, `emotion`, or other CSS-in-JS library is installed.

**Tailwind is installed and configured in the build pipeline but is not used anywhere in the actual UI code.** Evidence:
- `app/globals.css` contains no `@tailwind`, `@import "tailwindcss"`, or `@theme` directive (confirmed by reading the full 36-line file — it is pure hand-written CSS).
- A full-repo grep for Tailwind-shaped class names (`flex`, `grid`, `p-`, `m-`, `bg-`, `text-`, `w-`, `h-` inside `className="..."`) across every `.tsx` file returned zero matches — every `className` in the codebase references a CSS Module identifier (`styles.xxx`) instead.
- No `tailwind.config.*` file exists (Tailwind v4 doesn't strictly require one, but there is also no CSS-based `@theme` block substituting for it).

### Design tokens

Only two CSS custom properties are declared as global tokens, in `app/globals.css:1-5`:
```css
:root {
  color-scheme: light;
  --app-background: #f4f1ec;
  --app-foreground: #151515;
}
```
Two font-family custom properties are injected at runtime by `next/font` in `app/layout.tsx:7-14` (`--font-geist-sans`, `--font-geist-mono`) and consumed at `app/globals.css:21`.

Three scoped custom properties exist locally inside one CSS rule for the experience-timeline connector dot, `PortfolioSection.module.css:161-165`:
```css
--timeline-dot-size: 0.65rem;
--timeline-dot-offset: 0.4rem;
--timeline-dot-center: calc(
  var(--timeline-dot-offset) + (var(--timeline-dot-size) / 2)
);
```
No Tailwind theme extension exists (no config file, no `@theme` block — see above). There is no centralized color, spacing, radius, or shadow token system; all such values are hardcoded per-rule as literal hex/rgba/rem values throughout `PortfolioSection.module.css`.

### Font-size values

All 21 `font-size` declarations in the codebase live in one file, `components/portfolio/PortfolioSection.module.css`. 16 distinct values, sorted descending by occurrence count:

| Font-size value | Occurrences | Example line(s) |
|---|---|---|
| `0.86rem` | 5 | `PortfolioSection.module.css:72,224,348,524,827` |
| `1rem` | 2 | `PortfolioSection.module.css:247,746` |
| `clamp(3.125rem, 8vw, 5rem)` | 1 | `PortfolioSection.module.css:19` |
| `clamp(1rem, 1.4vw, 1.125rem)` | 1 | `PortfolioSection.module.css:86` |
| `1.35rem` | 1 | `PortfolioSection.module.css:127` |
| `0.79rem` | 1 | `PortfolioSection.module.css:156` |
| `0.96rem` | 1 | `PortfolioSection.module.css:215` |
| `0.83rem` | 1 | `PortfolioSection.module.css:230` |
| `0.76rem` | 1 | `PortfolioSection.module.css:364` |
| `1.08rem` | 1 | `PortfolioSection.module.css:510` |
| `1.5rem` | 1 | `PortfolioSection.module.css:514` |
| `0.92rem` | 1 | `PortfolioSection.module.css:628` |
| `clamp(0.98rem, 1.25vw, 1.08rem)` | 1 | `PortfolioSection.module.css:644` |
| `0.78rem` | 1 | `PortfolioSection.module.css:739` |
| `0.8rem` | 1 | `PortfolioSection.module.css:802` |
| `clamp(1.05rem, 2vw, 1.2rem)` | 1 | `PortfolioSection.module.css:816` |

### Text color / background pairs and contrast

Backgrounds in this design are translucent (`rgba(255,255,255,0.62-0.94)`) layered over a gradient body background (`app/globals.css:22-25`, resolving to roughly `#f6f3ee`–`#f9f7f4` where cards sit), so the pairings below use the approximate rendered background color for each context and should be read as close estimates, not pixel-exact renders.

| Text color | Paired background (approx.) | Context | Contrast ratio (approx.) |
|---|---|---|---|
| `#4e4943` | `#f9f7f4` (card surface) | `.timelineCompany`, `.lightboxLink`(n/a—see below) — `PortfolioSection.module.css:223,363` | **8.33:1** |
| `#59524c` | `#f9f7f4` (card surface) | `.socialLink`, `.toolIconChip`, `.projectAction` icon color — `PortfolioSection.module.css:47,310,535` | **7.18:1** |
| `#5f5a54` | `#f9f7f4` (card surface) | `.heroDescription`, `.gallerySubtitle` — `PortfolioSection.module.css:85,643` | **6.38:1** |
| `#666059` | `#f9f7f4` (card surface) | `.locationText` — `PortfolioSection.module.css:246` | **5.81:1** |
| `#6d665f` | `#f9f7f4` (card surface) | `.projectMeta`, `.studyCaseDescription` — `PortfolioSection.module.css:523,627` | **5.28:1** |
| `#746d65` | `#f9f7f4` (card surface) | `.timelineDescription` — `PortfolioSection.module.css:229` | **4.77:1** |
| `#7e776f` | `#f9f7f4` (card surface) | `.timelineDate` — `PortfolioSection.module.css:155` | **4.13:1** (passes AA for body text at 4.5:1 only marginally / borderline fails depending on exact rendered background) |
| `#000000` | photographic background image (`featured-project.png`) | `.studyCaseCardFeatured .cardTitle/.studyCaseTitle/.studyCaseMeta/.studyCaseDescription` — `PortfolioSection.module.css:584-597` | **Not computable** — background is a busy screenshot, not a flat color; ratio varies by region and is unverifiable without the underlying image's own contrast against black text |
| `#f7f5f1` / `rgba(247,245,241,0.76-0.88)` | `rgba(24,20,16,0.64)` lightbox overlay over black | `.lightboxCaption`, `.lightboxMeta`, `.lightboxLink` — `PortfolioSection.module.css:796,801,826` | **~15:1** (approx., against near-black overlay) |
| `#ffffff` | `rgba(24,20,16,0.64)` lightbox overlay | `.lightboxLink:hover` — `PortfolioSection.module.css:833` | **~16.7:1** (approx.) |

### Bento grid (top section) — exact CSS

`PortfolioSection.module.css:90-115`:
```css
.layout {
  display: grid;
  grid-template-columns: minmax(0, 1.05fr) minmax(0, 1fr) minmax(0, 1.1fr);
  gap: clamp(1rem, 1.6vw, 1.5rem);
  align-items: stretch;
}

.column {
  display: grid;
  min-width: 0;
  gap: clamp(1rem, 1.6vw, 1.5rem);
  height: 100%;
  align-content: start;
}

.columnPrimary {
  grid-template-rows: 1fr;
}

.columnMiddle {
  grid-template-rows: auto auto minmax(0, 1fr);
}

.columnRight {
  grid-template-rows: minmax(0, 1fr) auto;
}
```
Responsive overrides at `PortfolioSection.module.css:913-931` (2-column at ≤1080px) and `PortfolioSection.module.css:938-957` (1-column stack at ≤767px).

### Gallery layout — exact CSS

Implemented as a **CSS multi-column layout** (`columns` property), not CSS Grid and not a JS masonry library. `PortfolioSection.module.css:648-659`:
```css
.galleryGrid {
  columns: 4 16rem;
  column-gap: clamp(1rem, 1.6vw, 1.5rem);
  margin: 0;
  padding: 0;
  list-style: none;
}

.galleryItem {
  break-inside: avoid;
  margin-bottom: clamp(1rem, 1.6vw, 1.5rem);
}
```
Responsive column-count overrides: 3 columns at ≤1080px (`PortfolioSection.module.css:933-935`), 2 columns at ≤767px (`PortfolioSection.module.css:968-970`), 1 column at ≤540px (`PortfolioSection.module.css:1005-1009`).

---

## 7. Components

| Path | Exported name | Props interface | Lines | Used >1×? |
|---|---|---|---|---|
| `components/portfolio/PortfolioSection.tsx` | `PortfolioSection` (default) | `interface PortfolioSectionProps { data: PortfolioData }` (`:11-13`) | 107 | No — single call site (`app/page.tsx:10`) |
| `components/portfolio/components/DesignGallery.tsx` | `DesignGallery` | `interface DesignGalleryProps { gallery: DesignGallerySection }` (`:10-12`) | 48 | No |
| `components/portfolio/components/ExperienceCard.tsx` | `ExperienceCard` | `interface ExperienceCardProps { title: string; entries: ExperienceEntry[] }` (`:4-7`) | 41 | No |
| `components/portfolio/components/GalleryCard.tsx` | `GalleryCard` | `interface GalleryCardProps { item: DesignGalleryItem; onOpen: () => void; index: number }` (`:6-10`) | 56 | **Yes — rendered 47×** (once per gallery item, `DesignGallery.tsx:30-34`) |
| `components/portfolio/components/GalleryLightbox.tsx` | `GalleryLightbox` | `interface GalleryLightboxProps { items: DesignGalleryItem[]; selectedIndex: number \| null; onClose: () => void; onSelect: (index: number) => void }` (`:11-16`) | **211** | No | 
| `components/portfolio/components/LocationCard.tsx` | `LocationCard` | `interface LocationCardProps { location: PortfolioLocation }` (`:5-7`) | 30 | No |
| `components/portfolio/components/LocationMap.tsx` | `LocationMap` | `interface LocationMapProps { lat: number; lng: number; label: string; zoom: number }` (`:3-8`) | 45 | No |
| `components/portfolio/components/ProjectCard.tsx` | `ProjectCard` | `interface ProjectCardProps { project: ProjectItem; openProjectLabel: string; variant: "compact" \| "tile" }` (`:7-11`) | 54 | **Yes — rendered 5×** (3 featured + 2 secondary, `PortfolioSection.tsx:63-71,90-98`) |
| `components/portfolio/components/SocialLinks.tsx` | `SocialLinks` | `interface SocialLinksProps { links: SocialLinkItem[] }` (`:7-9`) | 49 | No |
| `components/portfolio/components/StackToolsCard.tsx` | `StackToolsCard` | `interface StackToolsCardProps { stackTools: StackToolsSection }` (`:5-7`) | 48 | No |
| `components/portfolio/components/StudyCaseCard.tsx` | `StudyCaseCard` | `interface StudyCaseCardProps { project: FeaturedStudyCase; openProjectLabel: string }` (`:6-9`) | 58 | No |
| `components/portfolio/components/ToolsIconList.tsx` | `ToolsIconList` | `interface ToolsIconListProps { items: StackToolItem[] }` (`:7-9`) | 109 | **Yes — rendered 3×** (frontend/backend/designer groups, `StackToolsCard.tsx:33-44`) |

Non-component utility/data modules (not React components, listed for completeness):
- `components/portfolio/iconMap.ts` (33 lines) — icon-key → `lucide-react` component map + `hasMappedIcon`/`isImageIconSource` helpers.
- `components/portfolio/socialIconMap.tsx` (74 lines) — social-key → icon component map, including one inline `BehanceIcon` SVG component.
- `components/portfolio/types.ts` (99 lines) — all shared TypeScript interfaces.

**Flag: component over 200 lines** — `components/portfolio/components/GalleryLightbox.tsx` at **211 lines** is the only file exceeding the 200-line threshold.

---

## 8. Known defects to confirm

### "Based In" map card

- **Library:** No JS mapping library (`leaflet`, `react-leaflet`, `mapbox-gl`, `google-maps` SDK) is installed or imported anywhere. `components/portfolio/components/LocationMap.tsx:37-43` renders a raw `<iframe>` pointing at OpenStreetMap's public embed endpoint:
```tsx
<iframe
  className={styles.locationMapFrame}
  src={src}
  title={`Map showing ${label}`}
  loading="lazy"
  referrerPolicy="no-referrer-when-downgrade"
/>
```
  where `src` (`LocationMap.tsx:30-34`) is built as `https://www.openstreetmap.org/export/embed.html?bbox=...&layer=mapnik&marker=...`.
- **Tiles fetched at runtime:** Yes — the iframe loads `www.openstreetmap.org` live over the network at render time; nothing is pre-rendered, cached, or bundled. If that request is blocked, slow, or fails (e.g., no network, CSP restriction, ad-blocker, or OSM rate-limiting), the map area falls back to its CSS placeholder.
- **API key / env var:** **None required and none present.** A full-repo grep for `process.env`, `NEXT_PUBLIC_`, `API_KEY`, `apiKey`, `MAPBOX`, `GOOGLE_MAPS` (case-insensitive) returned zero matches anywhere in the repository — there is no `.env` file and no environment-variable indirection at all for this feature.
- **Gray container fallback:** `PortfolioSection.module.css:250-257` sets the wrapping `<figure>` (`.locationMap`) background to a flat `#e7e2db` gray, sized via `aspect-ratio: 16/9`, with the iframe (`.locationMapFrame`, `:263-268`) set to `width:100%; height:100%; border:0`. If the iframe's own document (tiles, styling) fails to paint over its own background while OSM's attribution overlay (rendered inside the iframe by OSM itself, not by this codebase) still shows, the described "gray box with only attribution visible" is exactly what this CSS/markup combination would produce.

### Header bio phrase

- Confirmed verbatim at `data/portfolioData.json:4`: `"description": "Fullstack developer focused on building clear, functional, and scalable digital products. ..."` — contains the exact phrase **"Fullstack developer"** at the start of the string.

### "Typo" project ↔ "type-forge" repo

- `data/portfolioData.json:196-202` defines the Typo project card with `"href": "https://typo-nu.vercel.app/"` — a live Vercel deployment URL. **No reference to a GitHub repository named "type-forge" (or any repository name at all) exists anywhere in this codebase** — no README mention, no package.json `repository` field pointing to it, no comment. This cannot be confirmed or denied from repository contents alone; it would require checking the external deployment/GitHub account, which is out of scope for a static repo audit.

### Alexandria screenshot cropped mid-content

- Confirmed visually. The Alexandria featured-study-case card does not render any of the four Alexandria-specific gallery screenshots (`alexandria-bento-01.png`, `alexandria-beta-landing.png`, `alexandria-import-file-modal.png`, `alexandria-settings-modal.png`) at all — its dedicated `<Image>` is commented out (`StudyCaseCard.tsx:33-41`). Instead the entire card uses a generic photographic CSS background at `PortfolioSection.module.css:557-559`:
```css
.studyCaseCardFeatured {
  position: relative;
  overflow: hidden;
  background:
    url("/images/portfolio/featured-project.png") center / cover no-repeat;
}
```
  Opening `public/images/portfolio/featured-project.png` directly shows a note-taking app screenshot ("Digital Brain") whose right-hand content column and heading ("Derivat[ives]") are **cut off mid-word/mid-content** at the image's right edge — and because the CSS uses `background-size: cover`, the visible portion is cropped further at render time depending on the card's actual aspect ratio. This image is also byte-identical to the unused `public/images/portfolio/freatured-project.png` (see Section 5).

### Spanish-language gender-agreement errors

- **Not present.** A case-insensitive full-repo grep for `regalos`, `mesa`, and `gift`/`registry` returned zero matches. There is no gallery item, filename, or JSON string anywhere containing "Lo mesa de regalos perfecto" or any other Spanish-language copy in gallery captions/filenames — all gallery `title`/`category`/`alt` text in `data/portfolioData.json` is in English.

---

## 9. SEO and metadata

This is a Next.js App Router project, so there is **no static `index.html` file** to quote — `<head>` content is generated by Next.js from the `metadata` export in `app/layout.tsx` plus Next's own framework defaults (charset, viewport meta, etc., which are not visible as source code).

Verbatim `metadata` export (`app/layout.tsx:19-22`):
```ts
export const metadata: Metadata = {
  title: portfolioContent.profile.name,
  description: portfolioContent.profile.description,
};
```
This resolves at runtime to `title: "Hector Rivera"` and `description: "Fullstack developer focused on building clear, functional, and scalable digital products. ..."` (from `data/portfolioData.json:3-4`).

`<html>` tag verbatim (`app/layout.tsx:30`): `<html lang="en">` — **`lang` attribute is present** (`en`).

Presence/absence checklist:
- **`<meta name="description">`** — Present, generated from the `metadata.description` field above.
- **`og:title`** — **Not present.** No `openGraph` key exists in the `metadata` object.
- **`og:description`** — **Not present.**
- **`og:image`** — **Not present.**
- **`twitter:card`** — **Not present.** No `twitter` key exists in the `metadata` object.
- **Canonical `<link>`** — **Not present.** No `alternates.canonical` field in `metadata`, no manual `<link rel="canonical">` anywhere.
- **Favicon** — Present via Next.js App Router file convention: `app/icon.svg` (auto-served as the site favicon; no explicit `<link rel="icon">` needed or written in source).
- **`sitemap.xml`** — **Not present.** No static file and no `app/sitemap.ts`/`app/sitemap.xml/route.ts` generator.
- **`robots.txt`** — **Not present.** No static file and no `app/robots.ts` generator.

---

## 10. Accessibility baseline

- **Images missing `alt`:** **0.** All 5 rendered `<Image>` elements in the codebase (`ToolsIconList.tsx:29`, `GalleryLightbox.tsx:165`, `ProjectCard.tsx:32`, `GalleryCard.tsx:26`, `SocialLinks.tsx:32`) supply an `alt` prop (2 are intentionally empty `alt=""` on decorative icons paired with `aria-hidden="true"`, which is correct usage, not a defect).
- **Interactive elements without accessible names:** **0 found.** Every `<a>` and `<button>` in the codebase carries either visible text content or an explicit `aria-label`: `SocialLinks.tsx:28` (`aria-label={link.ariaLabel}`), `ToolsIconList.tsx:67` (`aria-label={item.ariaLabel}`), `ProjectCard.tsx:29` (`aria-label={...}`), `StudyCaseCard.tsx:23` (`aria-label={...}`), `GalleryCard.tsx:44` (`aria-label={...}`), `GalleryLightbox.tsx:147,159,180` (close/prev/next `aria-label`s), and the lightbox's "Open project" link (`GalleryLightbox.tsx:200`) has visible text content. The `LocationMap.tsx:40` `<iframe>` has a `title` attribute, which serves as its accessible name.
  - **Caveat / near-miss:** `StudyCaseCard.tsx:26` renders `<h2 className={styles.cardTitle}>{project.label}</h2>` where `project.label` is `""` (`data/portfolioData.json:185`) — this produces an **empty, non-visually-hidden `<h2>` element** in the DOM. It is not an interactive element, so it doesn't count against the "interactive elements without accessible names" tally above, but it is an accessibility defect: a heading with no accessible text content.
- **Heading levels used:** `h1`, `h2`, `h3` only. No `h4`, `h5`, or `h6` appears anywhere in the codebase.
- **Heading order:** Sequentially valid in DOM order — `h1` (`PortfolioSection.tsx:35`, page title) → `h2` (`ExperienceCard.tsx:15` "Experience") → `h3`×5 (`ExperienceCard.tsx:28`, one per timeline entry) → `h2` (`LocationCard.tsx:13` "Based In") → `h2` (`StackToolsCard.tsx:30` "Stack & Tools") → `h3`×3 sr-only group labels (`StackToolsCard.tsx:39`) → `h2` sr-only (`PortfolioSection.tsx:59` "Featured Projects") → `h3`×3 (`ProjectCard.tsx:42`) → `h2` (`StudyCaseCard.tsx:26`, empty — see above) → `h3` (`StudyCaseCard.tsx:44` "Alexandria") → `h2` sr-only (`PortfolioSection.tsx:86` "More Projects") → `h3`×2 (`ProjectCard.tsx:42`) → `h2` (`DesignGallery.tsx:21` "Design Gallery") → `h3`×47 (`GalleryCard.tsx:19`, one per gallery item) → `h3` (`GalleryLightbox.tsx:190`, conditionally rendered only while the modal is open). **No level is skipped** at any point (never jumps from `h1` to `h3`, or `h2` to `h4`, etc.).
- **Explicit `tabindex` (`tabIndex` in JSX):** exactly **1 occurrence** in the entire codebase — `components/portfolio/components/ToolsIconList.tsx:89`: `tabIndex={hasIcon ? 0 : undefined}`, applied to a `<div role="img">` fallback wrapper only when it has no `href` (i.e., a tool entry with an icon but no link). No negative (`-1`) or positive (`>0`) tabindex values appear anywhere.
